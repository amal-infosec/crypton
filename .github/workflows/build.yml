name: Build & Release

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build_linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libsecret-1-dev
      
      - name: Build Linux Release
        run: flutter build linux --release
        
      - name: Create AppImage (Mock Step)
        run: |
          echo "AppImage creation would happen here. For now, we'll zip the bundle."
          cd build/linux/x64/release/bundle
          zip -r ../../../../crypton-linux.zip .
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: crypton-linux
          path: crypton-linux.zip

  build_windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Build Windows Release
        run: flutter build windows --release
        
      - name: Create MSIX
        run: flutter pub run msix:create
        
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: crypton-windows
          path: build/windows/x64/runner/Release/crypton.msix
